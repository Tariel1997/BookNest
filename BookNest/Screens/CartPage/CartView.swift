import SwiftUI

struct CartView: View {
    var body: some View {
        NavigationView {
            Text("This is the Cart View")
                .font(.title)
                .navigationTitle("Cart")
        }
    }
}
