import SwiftUI

struct ProfileView: View {
    var body: some View {
        NavigationView {
            Text("This is the Profile View")
                .font(.title)
                .navigationTitle("Profile")
        }
    }
}
