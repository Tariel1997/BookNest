import SwiftUI

struct BooksView: View {
    var body: some View {
        NavigationView {
            Text("This is the Books View")
                .font(.title)
                .navigationTitle("Books")
        }
    }
}
